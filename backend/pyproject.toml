[project]
name = "backend"
version = "0.1.0"
description = "Behavioral Authentication Backend with Isolation Forest ML"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "scikit-learn>=1.4.0",
    "joblib>=1.3.2",
    "numpy>=1.26.0",
    "pydantic>=2.0.0"
]
